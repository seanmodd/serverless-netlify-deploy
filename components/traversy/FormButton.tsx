import { Button, IconButton } from '@chakra-ui/button';
import { MyButton } from './Styled';

const FormButton = ({ onAdd, showAdd, text }) => (
  <div>
    <Button colorScheme="whatsapp">Form Button</Button>
  </div>
);

export default FormButton;
